import React from 'react';
import * as BookList from '../containers/BookList';
import * as BookForm from './BookForm';

const App = () => (
  <div>
    <BookList />
    <BookForm />
  </div>
);

export default App;
